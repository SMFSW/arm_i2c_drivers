/*!\file $CPNT$_proc.c
** \author SMFSW
** \date 2017
** \copyright MIT (c) 2017, SMFSW
** \brief $CPNT$ Driver procedures code
** \details $CPNT$:
**/
/****************************************************************/
#include "$CPNT$.h"
#include "globals.h"

#if defined(HAL_I2C_MODULE_ENABLED)
#if defined(I2C_$CPNT$)
/****************************************************************/
// std libs
//#include <math.h>
//#include <stdlib.h>
#include <string.h>
/****************************************************************/


$CPNT$_proc $CPNT$ = { 0, { 0, 0 } };


/****************************************************************/


__WEAK FctERR $CPNT$_Init_Sequence(void)
{
	FctERR err;
	
	// get ID & check against values for $CPNT$
	err = $CPNT$_Get_ChipID(&$CPNT$.cfg.Id);
	if (err)								{ return err; }
	if ($CPNT$.cfg.Id != $CPNT$_CHIP_ID)	{ return ERR_COMMON; }	// Unknown device
	
	$CPNT$.cfg.$CPNT$_param_1 = 1;
	
	return ERR_OK;
}


/****************************************************************/


__WEAK FctERR $CPNT$_handler(void)
{
	FctERR err;
	
	if (err)	{ return err; }
	
	#if defined(VERBOSE)
		printf("$CPNT$: FUBARED %d", $CPNT$.$CPNT$_val_1);
	#endif
	
	return ERR_OK;
}


/****************************************************************/
#endif
#endif
/****************************************************************/
